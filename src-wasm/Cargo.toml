[package]
name = "bodeguita-recommendations"
version = "0.1.0"
authors = ["Bodeguita Team"]
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
